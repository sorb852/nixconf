# { pkgs, ... }:

{
  # services.greetd = {
  #   enable = true;
  #   # useTextGreeter = true;
  #   # settings = {
  #   #   default_session = {
  #   #     command = "${pkgs.greetd}/bin/agreetd --cmd sway";
  #   #   };
  #   # };
  # };
}
