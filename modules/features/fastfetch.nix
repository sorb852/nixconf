{
  flake.homeModules.fastfetch = {
    programs.fastfetch = {
      enable = true;

      # NOTE: I'm putting ts on hold
      # Like jesus, I don't have ANY ideas for the logo
      # Hell even how it looks.
      # I guess since the last rices (the ULTRAKILL rice thing)
      # fastfetch was so goated, I'm putting aside anything
      # less than it
      #
      # cant lie though that fetch was cool asf
      #
      # actually no I TAKE THAT BACK
      # IT WAS BEATIFUL LIKE ANTENNAS TO HEAVEN
      #
      # ULTRAKILL IS MY ONE AND ONLY LOVE
      # NULLSCAPES IS JUST LARP MATERIAL
      # I ADMIT I LARP GEOMETRY DASH
      # I ONLY THOUGHT NULLSCAPES LOOKED COOL

      # settings = {
      #   logo = {
      #     type = "small";
      #   };
      # };
    };
  };
}
